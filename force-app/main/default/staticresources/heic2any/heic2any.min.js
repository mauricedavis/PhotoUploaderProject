/*! heic2any shim – replace with full library if needed.
   Exposes window.heic2any({blob,toType}) -> Promise<Blob> (not real convert here).
*/
window.heic2any = window.heic2any || (function(){
  return function(opts){ return Promise.reject(new Error("heic2any library not bundled. Upload a full heic2any.min.js as Static Resource if you need HEIC conversion.")); }
})();